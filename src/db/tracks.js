import axios from 'axios';

const saveTrackInDB = (track, id) => {
  // const date = new Date();
  // const data = { TaskName:'TaskName', TrackDate: date, TrackNote: 'nnnnoooootttteeee' }
  // axios.post(`http://localhost:50698/api/task/track/${id}`, data);
  // POST  api/task/track/{id} and data { TaskName, TrackDate, TrackNote } - add new track to user task
};

export { saveTrackInDB };
